"""tvpc_cameras_gui — PySide6 GUI for tvpc IP security cameras.

Reads / writes the same config file format as scripts/tvpc-cameras.sh
(`~/.config/tvpc/cameras.conf`, one `NAME|URL|USER|PASS|NOTES` per line),
so the CLI script and the GUI stay interchangeable.
"""
