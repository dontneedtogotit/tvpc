# HTPC appliance: never suspend, never act on the (non-existent) lid.
# Drop-in so distro/package updates to logind.conf still apply.
[Login]
HandleLidSwitch=ignore
HandleLidSwitchExternalPower=ignore
HandleLidSwitchDocked=ignore
HandlePowerKey=poweroff
IdleAction=ignore
