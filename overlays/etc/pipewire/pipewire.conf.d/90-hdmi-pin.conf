context.modules = [
    # Disable Wi-Fi scanning when wired connection is active
    {   name = libpipewire-module-alsa-sink
        args = {
            # Pin HDMI as preferred node
            node.name = "alsa_output.pci-0000_00_1f.3.hdmi-stereo-extra"
        }
    }
]